from pathlib import Path
# set the config and patams file path here, we will be reading these yaml files
# with read_yaml function in common.py

CONFIG_FILE_PATH = Path("config/config.yaml")

PARAMS_FILE_PATH = Path("params.yaml")